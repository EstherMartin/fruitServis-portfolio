---
export function getStaticPaths() {
  return [
    { params: { lang: "es" } },
    { params: { lang: "en" } }
  ];
}

const langHeader = Astro.request.headers.get("accept-language");
const userLang = langHeader?.startsWith("en") ? "en" : "es";

return Astro.redirect(`/${userLang}`);
---